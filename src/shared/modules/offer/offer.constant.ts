export const enum OfferLimit {
  Default = 60,
  MinPrice = 100,
  MaxPrice = 100000,
}

export const enum OfferSortType {
  CreatedAt = 'createdAt',
  Price = 'price',
}

export const OFFER_COLLECTION = 'offers';
